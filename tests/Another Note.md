# Another Note

This note exists so that `![[Another Note]]` in `test.md` points at something real.
Obsidian would transclude it. A PDF has nowhere to put a transclusion, so the
converter marks it instead.
